# SIDRCE Configuration
SIDRCE_EMIT=1
SIDRCE_STREAM=ops_telemetry.jsonl
SIDRCE_REPORT=sidrce_report.json

# Optional: AES-GCM encryption (base64-encoded 32-byte key)
# Generate with: python -c "import os,base64;print(base64.b64encode(os.urandom(32)).decode())"
# SIDRCE_AES_KEY=

# Batch/flush settings
SIDRCE_BATCH_SIZE=100
SIDRCE_FLUSH_SEC=3.0
SIDRCE_GZIP=0
SIDRCE_ROLL_BYTES=10485760

# Causal analysis
DMCA_MIN_ROWS_FOR_DISCOVERY=1000
REX_CAUSAL_REQUIRED=1
